name = "mini-tds"
main = "src/worker.ts"
compatibility_date = "2024-11-01"

# Пример привязки маршрутов. Удалите или измените под свои нужды.
# routes = [
#   { pattern = "example.com/*", zone_name = "example.com" }
# ]

# Если нужен KV для дополнительных конфигов — раскомментируйте и пропишите свой namespace.
# kv_namespaces = [
#   { binding = "CONFIG", id = "" }
# ]
